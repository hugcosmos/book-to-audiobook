fastapi>=0.115.0
uvicorn[standard]>=0.34.0
jinja2>=3.1.0
python-multipart>=0.0.18
pydantic>=2.0
pydantic-settings>=2.0
beautifulsoup4>=4.12
lxml>=5.0
pdfplumber>=0.11.0
pydub>=0.25.1
audioop-lts>=0.2.0; python_version >= "3.13"  # audioop removed in 3.13 (PEP 594)
sentencex>=0.6.0
aiofiles>=24.0
soundfile>=0.12.1
edge-tts>=6.1.0
httpx>=0.27.0
websockets>=12.0
# Default local TTS engine (mirrors config/settings.py).
# Apple Silicon → Qwen3 MLX; Intel Mac → Kokoro.
mlx-audio>=0.1.0; platform_machine == "arm64"
hf-transfer>=0.1.9; platform_machine == "arm64"
kokoro-onnx>=0.5.0; platform_machine != "arm64"
onnxruntime>=1.19.0; platform_machine != "arm64"
misaki>=0.9.0; platform_machine != "arm64"
pypinyin>=0.55.0; platform_machine != "arm64"
cn2an>=0.5.0; platform_machine != "arm64"
# Optional local TTS providers — not installed by default.
# Install with: pip install "book-to-audiobook[supertonic]"
#   supertonic>=1.3.0          # ONNX, 33 languages (needs onnxruntime)
# Install with: pip install "book-to-audiobook[cosyvoice]"
#   sherpa-onnx>=1.10.0        # ONNX/CPU, multilingual
#   modelscope>=1.18.0
#   huggingface-hub>=0.24.0
# Install with: pip install "book-to-audiobook[kokoro]"
#   (Apple Silicon only; Intel Mac gets kokoro by default)
#   kokoro-onnx>=0.5.0
#   onnxruntime>=1.19.0
