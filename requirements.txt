fastapi>=0.115.0
uvicorn[standard]>=0.34.0
jinja2>=3.1.0
python-multipart>=0.0.18
pydantic>=2.0
pydantic-settings>=2.0
beautifulsoup4>=4.12
lxml>=5.0
pdfplumber>=0.11.0
pydub>=0.25.1
sentencex>=0.6.0
aiofiles>=24.0
soundfile>=0.12.1
edge-tts>=6.1.0
httpx>=0.27.0
websockets>=12.0
mlx-audio>=0.1.0; sys_platform == "darwin" and platform_machine == "arm64"
hf-transfer>=0.1.9; sys_platform == "darwin" and platform_machine == "arm64"
# Optional local TTS providers — not installed by default to avoid pulling heavy
# native deps (onnxruntime) that lack wheels on some Python versions (e.g. 3.13).
# Install with: pip install "book-to-audiobook[supertonic,cosyvoice]"
#   supertonic>=1.3.0          # ONNX, 33 languages (needs onnxruntime)
#   sherpa-onnx>=1.10.0        # CosyVoice ONNX/CPU runtime
#   modelscope>=1.18.0         # CosyVoice model download (preferred in China)
#   huggingface-hub>=0.24.0    # CosyVoice model download fallback
